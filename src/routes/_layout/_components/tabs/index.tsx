import TabsShowcase from '#/features/components/tabs'
import Tabs from '#/features/components/tabs'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_layout/_components/tabs/')({
  component: TabsShowcase,
})
